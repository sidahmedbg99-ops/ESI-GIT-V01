# ── Backend URL (Django default port is 8000) ──────────────────────
VITE_API_URL=http://localhost:8000/api

# ── Toggle mock data: true = use mock, false = use Django backend ──
VITE_USE_MOCK=false
