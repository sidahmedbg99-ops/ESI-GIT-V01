*ADMIN:
    #Dashboard:
        ?Completion Rate
        ?Pending Meetings
        ?Tasks
        ?Project state
    #Users:
        #AddUser:
            -Password in the form
            -"Choose" from the dropdown menu of specialty
            -Matricule for staff Users

            !a statistic for users who didnt log in their first time
        #EditUser:
            -Password field
        #ImportUsers:
            check it with a wrong file then correct file
    #Groups:
        !if the groups all are full , and there is < 3 students without a group
        -Type de project changes with the actual type
        +schedule presentation date ui
    #Platform Analytics:
        ?how does student activity work
        ?success rate / if its any project >10 then it will always be 100% => no need for it
        ?what is students at risk
        ?grading patterns
        ?usage trends
        ?grade trends
        ?active
        !characters not showing correctly when exporting data
    #Archive:
        -AvgGrade_Stat change the name
        +add level filter
        +add by specific supervisor (with pagination)
        +add by student (with pagination)
    #Settings:
        +grading formula gui
        +jury toggle dosent work
        +contact email isnt for forget password anymore but can be kept for other stuff
        +system for auto increment the year
        ?Type de project
        check the specialty adding system
        +notification system not implemented , admin cant send notifications
    #Profile:
        !add a tag for teacher in case user is both admin+user
        -no need for the tid in the profile
        +change password dosent work

*TEACHER:
    #Dashboard:
        ?does supervised groups have archived ones
        ?Completion Rate , same qst in admin
        ?how is Average Progress calculated
        ?Tasks by priority and Task state
        !move pending meetings up instead of task state , more frequent use        
    #MyGroups:
        +groups arent showing their title
        ?how is project progress calculated
        !remove supervision requests , its already in requests
        
        #CLICKING ON A GROUP:
            ?what is the in progress flag
            -ID project
            !github should be already there if the student uploaded their repo (didnt check)
            ?erreur de planification when creating a meeting with a any date why is that

    #Meetings:
        +"no pending supervision requests" if there is no meetings
    #Requests:
        !red pop up if there is new stuff (the new stuff should be on notifications too, so do we skip this and let him read from the notifications?)
        #Supervision requests:
            -Code:PRJ-1 no need for it displayed 
            +should display the project title not team title which needs to be gone
            +notification didnt pop up for the request but accepting it shows the noti for sutdent (just this or the whole system is bugged?)

    #Jury:
        +if he isnt the president he cant input grades but he can see the form and result later
    #Archive:
        +same filters additions for admin
    #Profile:
        ?Departement "Informatique"
        maybe because of seed data
        +change pass not working

*STUDENT:
    #Dashboard:
    #Groups:
        #Create a group:
            !supervisors with the flag firstlogin true musnt be on the supervisors list
            !choices need to be fixed (e.g. no frontend on robotics)
            -Team name
        #MyGroup:
            - 1/6 members , 6 members isnt the required but the max , however
            !min is 3 which should be noted if <3 members on the group
            -the current group card have duplicates of team name
            ?invitation code cant be guessed for security measures
            +private repo possibility
            +Project validation should check if you have any documents uploaded , if so send the request with the "ProjectDocs" (supervisor should be able to see them always since he also is working along the students so if there is a request he just checks the projectdocs) else tell the user that he needs documents first
            +cant send a request
    #Meetings:
        +page should be inaccessible if no supervisor
        +Any meeting after the end or before the academic year ending / starting should be invalid 
        +encadreur avatar bubble and name isnt showing , just __
    #Tasks:
        +should be more specific in the error when creating a task and the required fields + dont let the pop window go if the creation is incorrect
        +same logic for meeting dates
    #Archive:
    #Profile:
        not loading

*PUBLIC:
    +forget password dosent work
    +in case for first login new password must be strong (current sys can be even 1 char)




------------------------------------unrelated improvments-------------------------------------------------
-have a notification page where you can browse all your notifications , if we must we can add a cleaning system after 2 years or so
-there seems to be time issue with the tokens , after a certain time you have to disconnect to use your functions again , worth looking into
----------------------------------------------------------------------------------------------------------




-------------------------------------------FLAGS----------------------------------------------------------
F1: Student specialty — how do they get it?
F2: Staff department field may be useless
F3: current_academic_year bump logic — "Close year & advance" button
F4: jury_page_visible check in TeacherJuryListView
F5: i18n translations — set up i18next then switch notifications to keys
F6: GitHub private repo support via token in .env
F7: Supervisor.jsx — show rejection status clearly (rejected = can't resend)
F8: Login.jsx — save generated password in memory for first login flow
F9: Groupe.jsx — project type dropdown + Other free text option
F10: StudentGroupStatusView + PublicSettingsView — confirm with teammate
F11: Admin schedule creation UI (Part 6 above)
F12: simpleeval — swap eval() in grading engine for proper sandbox
F13: Supervisor request rejected — make clear in UI student can't resend
F14: /student/encadreur and /teacher/requests sidebar — decide if permanent or contextual
F15: rading form hardcoded — needs to read GradingFormula.labels dynamically. 
     Endpoint: PATCH /api/jury/teacher/grades/<pid>/
F16:Email backend — swap console to Gmail SMTP when ready
F17: github_url save broken — hits student-only endpoint. Needs teacher-side PATCH
F18: Cancel has no reason prompt — cancellation_reason field exists on model but nothing sends it
----------------------------------------------------------------------------------------------------------




===========================================LEGEND=========================================================
* --> roles
# --> pages
- --> delete
+ --> add / fix
? --> why do we need it
! --> ideas / improvments / what ifs
==========================================================================================================




Verification order from claude:

    Auth flow

        Login works (normal + first-login password change)
        Role redirect works (student/teacher/admin go to right dashboard)

    Admin

        Users CRUD (create student, create staff, edit, no password change for others)
        Groups list, sidebar panel (students without group)
        Jury assignment gate (only shows for final_submission_approved groups)
        Archive page, tech tags display correctly
        Settings page, Analytics page

    Teacher

        Dashboard loads with stats
        Groups page, supervisor requests tab
        Meetings (accept/reject)
        Jury page (grades — will be hardcoded for now, F15 is a flag)
        Profile (availability toggle + password change)

    Student

        Group page (create/join flow, submission to supervisor)
        Supervisor page (send request, see status)
        Meetings (request, cancel)
        Tasks
        Archive

    Things expected to NOT work yet (flags)

        F15: Jury grading formula dynamic form
        F17: Teacher saving github_url
        F19: Meeting cancel reason prompt